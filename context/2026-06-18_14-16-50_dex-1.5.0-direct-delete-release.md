# Context Packet: Dex 1.5.0 direct delete release

- Time: 2026-06-18_14-16-50
- User request: 提交并推送远程，然后重新打包发新版 1.5.0.
- Scope: `D:\Skye\codex-enhance` current `main` branch.
- Commit scope:
  - Include direct-delete behavior from prior turn.
  - Include version bump to `1.5.0`.
  - Exclude pre-existing `.gitignore` dirty change unless later evidence proves it belongs to this task.
- Version sources:
  - `Cargo.toml` workspace package version.
  - `apps/codex-plus-manager/package.json`.
  - `apps/codex-plus-manager/src-tauri/tauri.conf.json`.
  - `apps/codex-plus-manager/package-lock.json`.
  - `apps/codex-plus-manager/src/lib/tauri-api.ts` Web preview mock version.
  - `Cargo.lock` generated package entries.
- Release boundary:
  - User explicitly requested commit/push and local repackaging.
  - Do not claim GitHub Release/updater publication unless tag, release, workflow, and assets are actually created and verified.
- Validation plan:
  - Strict diff review before commit.
  - Rust injection tests and broader relevant checks.
  - Typecheck manager frontend.
  - Tauri build and artifact enumeration with hashes.
  - Git commit, push, and remote branch verification.
- RTK evidence:
  - `meta-ruli rtk -- ...` reports third-party `rtk` not found and falls back to raw execution.
